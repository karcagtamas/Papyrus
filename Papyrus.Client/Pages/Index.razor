@page "/"

<PageTitle>@PapyrusContext.ApplicationName</PageTitle>

@code {
    [Inject]
    private NavigationManager NavigationManager { get; set; } = null!;

    protected override void OnInitialized()
    {
        base.OnInitialized();

        NavigationManager.NavigateTo("/dashboard");
    }
}