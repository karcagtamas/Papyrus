@page "/groups/{GroupId:int}/logs"
@using KarcagS.Shared.Helpers
@using Papyrus.Shared.DTOs.Groups.ActionsLogs
@attribute [Authorize]

<GroupPageTitle PageName="Logs" />

<div class="page-div">
    @if (ObjectHelper.IsNotNull(DataSource) && ObjectHelper.IsNotNull(Config))
    {
        <ListTable T="GroupActionLogDTO" TKey="long" DataSource="DataSource" Config="Config" />
    }
</div>