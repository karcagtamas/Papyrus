@using MudBlazor.Utilities
@inject IStringLocalizer<ColorPickerDialog> L

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6" Color="Color.Primary">
            @L["Title"]
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudColorPicker PickerVariant="PickerVariant.Static" @bind-Value="ColorValue" />
    </DialogContent>
    <DialogActions>
        <MudButton Variant="Variant.Text" OnClick="@(() => Dialog.Close())">@L["CloseAction"]</MudButton>
        <MudButton Variant="Variant.Text" Color="Color.Primary" OnClick="@(() => Dialog.Close(ColorValue))">@L["ApplyAction"]</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter]
    private MudDialogInstance Dialog { get; set; } = default!;

    [Parameter]
    public MudColor ColorValue { get; set; } = "AAAAAA";
}
